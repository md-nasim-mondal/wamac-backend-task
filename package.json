{
  "name": "novapay-backend",
  "version": "1.0.0",
  "description": "Production-grade fintech backend using microservice architecture",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=account-service\" \"npm run dev --workspace=transaction-service\" \"npm run dev --workspace=ledger-service\" \"npm run dev --workspace=fx-service\" \"npm run dev --workspace=payroll-service\" \"npm run dev --workspace=admin-service\"",
    "start": "docker-compose up --build",
    "test": "jest --runInBand",
    "lint": "npm run lint --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "services/*"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/supertest": "^2.0.14",
    "concurrently": "^9.2.1",
    "jest": "^29.7.0",
    "supertest": "^7.2.2",
    "ts-jest": "^29.1.0"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.8.0",
    "@opentelemetry/auto-instrumentations-node": "^0.52.1",
    "@opentelemetry/exporter-jaeger": "^1.25.1",
    "@opentelemetry/sdk-node": "^0.52.1"
  }
}
